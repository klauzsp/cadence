# Cadence web

Next.js frontend for Cadence, configured for Monad testnet with wagmi, viem, RainbowKit, and Reown.

From the repository root:

```bash
pnpm install
pnpm dev
```

See the root `README.md` for environment variables, contract deployment, and MVP limitations.
